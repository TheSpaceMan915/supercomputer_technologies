# assignment3-task1 — π via Midpoint (OpenMP, C++98)

Parallel midpoint rule with OpenMP reduction.

Build (standalone):
```bash
cmake -S . -B build-a3
cmake --build build-a3
./build-a3/assignment3-task1 1000000
```
